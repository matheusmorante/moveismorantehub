import { createClient } from '@supabase/supabase-js';

const supabaseUrl = 'https://hkoxhourxwlddgsfdgws.supabase.co';
const supabaseKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imhrb3hob3VyeHdsZGRnc2ZkZ3dzIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzgxNTg5MzgsImV4cCI6MjA5MzczNDkzOH0.vCNJeoR4wDl1BqESiyNhKpgviwxcx0cim8Dbl6MvdJI';

const supabase = createClient(supabaseUrl, supabaseKey);

async function inspectMarketingTables() {
  const tables = ['marketing_assets', 'marketing_campaigns', 'marketing_templates', 'store_style_settings'];
  for (const table of tables) {
    const { data, error } = await supabase.from(table).select('*').limit(1);
    if (error) {
      console.log(`Tabela ${table}: NÃO EXISTE ou erro (${error.message})`);
    } else {
      console.log(`Tabela ${table}: EXISTE (${data.length} registros). Exemplo:`, data[0] || 'vazia');
    }
  }
}

inspectMarketingTables();
