const KEY = 'morante_post_generation_guidelines_v1';
export interface PostGenerationGuidelines { id: string; version: number; content: string; updatedAt: string; }
const initial: PostGenerationGuidelines = { id: 'global', version: 1, updatedAt: '2026-09-06', content: `DIRETRIZES GERAIS DE GERAÇÃO: Use somente as imagens explicitamente selecionadas. PRIMARY_IMAGE é a primeira imagem da primeira variação e recebe o maior destaque. SECONDARY_IMAGE é a segunda imagem da mesma variação, menor e próxima da principal. VARIATION_IMAGES pertencem apenas à galeria de variações, como cards menores com borda branca, cantos discretos e sem legendas. Nunca misture características entre variações. Se não houver segunda imagem ou outras variações, não invente nem reserve espaço vazio.` };
export const postGenerationGuidelines = { get: (): PostGenerationGuidelines => { try { return JSON.parse(localStorage.getItem(KEY) || '') || initial; } catch { return initial; } }, save: (content: string) => { const previous = postGenerationGuidelines.get(); const next = { ...previous, content, version: previous.version + 1, updatedAt: new Date().toISOString() }; localStorage.setItem(KEY, JSON.stringify(next)); return next; } };
