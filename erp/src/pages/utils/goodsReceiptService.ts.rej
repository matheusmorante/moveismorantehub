diff a/erp/src/pages/utils/goodsReceiptService.ts b/erp/src/pages/utils/goodsReceiptService.ts	(rejected hunks)
@@ -576,12 +576,21 @@ export const subscribeToGoodsReceipts = (callback: (items: GoodsReceipt[]) => vo
 
     load();
 
+    let debounceTimer: ReturnType<typeof setTimeout> | null = null;
+    const requestLoad = () => {
+        if (debounceTimer) clearTimeout(debounceTimer);
+        debounceTimer = setTimeout(() => {
+            load();
+        }, 3000);
+    };
+
     const channel = supabase.channel(`goods_receipts_${Date.now()}`)
-        .on('postgres_changes', { event: '*', schema: 'public', table: 'goods_receipts' }, load)
-        .on('postgres_changes', { event: '*', schema: 'public', table: 'goods_receipt_items' }, load)
+        .on('postgres_changes', { event: '*', schema: 'public', table: 'goods_receipts' }, requestLoad)
+        .on('postgres_changes', { event: '*', schema: 'public', table: 'goods_receipt_items' }, requestLoad)
         .subscribe();
 
     return () => {
+        if (debounceTimer) clearTimeout(debounceTimer);
         listeners = listeners.filter((cb) => cb !== callback);
         supabase.removeChannel(channel);
     };
